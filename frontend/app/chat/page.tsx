import ChatComponent from '../../components/ChatComponent';
import '../globals.css';

export default function ChatPage() {
    return (
        <main>
            {/* The component is self-contained and will fill the page */}
            <ChatComponent />
        </main>
    );
}